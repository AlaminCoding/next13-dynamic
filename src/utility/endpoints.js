export const HOST = `${process.env.NEXT_APP_DEVHOST}`;
export const baseUrl = `https://${process.env.NEXT_APP_DEVHOST}/api/v2`;
export const pageUrl = `${baseUrl}/pages`;
export const otherUrl = `${baseUrl}/others`;
